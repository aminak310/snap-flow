name: snapflow
description: A professional social media app (Instagram clone)

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # 🔥 Supabase (Backend + Auth + Database)
  supabase_flutter: ^2.12.4
  image_picker: ^1.2.1
  video_player: ^2.11.1


  # 📸 Image Picker (Gallery se image lene ke liye)
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true